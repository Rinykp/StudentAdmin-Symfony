{% extends 'dashboard/index.html.twig' %}

{% block title %}Edit Teacher{% endblock %}

{% block content %}

<div class="col-sm-9">    <h1>Edit Teacher</h1>

    {{ include('teacher/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-success" href="{{ path('app_teacher_index') }}">back to list</a>

    {{ include('teacher/_delete_form.html.twig') }}
</div>
{% endblock %}
